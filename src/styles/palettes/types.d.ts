declare global {
  namespace Palettes {
    interface Pattern {
      white: string;
      black: string;
      opacityGray: string;
      red: string;
      blue: string;
      mainText: string;
      subText: string;
      gray: string;
      loader: string;
    }
  }
}

export {};
